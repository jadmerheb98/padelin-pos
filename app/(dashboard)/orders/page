import { Topbar } from '@/components/layout/topbar';
import { recentOrders } from '@/lib/data';

export default function OrdersPage() {
  return (
    <section className="section-stack">
      <Topbar title="Orders" subtitle="Order history, receipts, and payment status" />
      <article className="card">
        <div className="card-head">
          <div className="card-title">Orders list</div>
          <div className="card-sub">Use this screen for reprints, refunds, and operational checks</div>
        </div>
        <div className="card-body">
          <table className="table">
            <thead>
              <tr>
                <th>Order</th>
                <th>Customer</th>
                <th>Total</th>
                <th>Payment</th>
                <th>Status</th>
              </tr>
            </thead>
            <tbody>
              {recentOrders.map((order) => (
                <tr key={order.number}>
                  <td>{order.number}</td>
                  <td>{order.customer}</td>
                  <td>{order.total}</td>
                  <td>{order.payment}</td>
                  <td>{order.status}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </article>
    </section>
  );
}
