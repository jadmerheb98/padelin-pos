export default function LoginPage() {
  return (
    <main className="login-shell">
      <section className="login-card">
        <div className="login-head">
          <span className="badge">PADELIN staff access</span>
          <h1>Sign in to PADELIN POS</h1>
          <p>Counter-ready access for reception, checkout, inventory, and reporting.</p>
        </div>

        <div className="login-body">
          <div className="field">
            <label>Email</label>
            <input className="input" placeholder="staff@padelin.com" />
          </div>

          <div className="field">
            <label>Password</label>
            <input className="input" type="password" placeholder="••••••••" />
          </div>

          <div className="field">
            <label>Role</label>
            <select className="select" defaultValue="Admin">
              <option>Admin</option>
              <option>Staff</option>
            </select>
          </div>

          <a className="button primary" href="/dashboard">Enter system</a>
          <div className="muted" style={{ fontSize: 13 }}>
            Starter file only. Replace this page with Supabase Auth or your preferred auth provider.
          </div>
        </div>
      </section>
    </main>
  );
}
